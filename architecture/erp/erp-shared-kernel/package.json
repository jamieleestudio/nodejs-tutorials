{
  "name": "@erp/shared-kernel",
  "version": "0.2.0",
  "description": "ERP (COLA-style) — shared kernel: pure TS domain primitives. Zero framework dependencies.",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
