{
  "name": "@erp/platform",
  "version": "0.2.0",
  "description": "ERP (COLA-style) — platform layer: reusable technical components (config, exception filter, logging interceptor). Depends only on shared-kernel.",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@erp/shared-kernel": "workspace:*",
    "@nestjs/common": "^12.0.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2"
  }
}
