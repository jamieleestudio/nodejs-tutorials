{
  "name": "architecture-distributed",
  "version": "0.1.0",
  "description": "Distributed architecture: non-CQRS + Task domain + multi-process HTTP.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/main.ts",
    "start:worker": "tsx src/worker.ts",
    "typecheck": "tsc --noEmit",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init"
  },
  "dependencies": {},
  "devDependencies": {}
}