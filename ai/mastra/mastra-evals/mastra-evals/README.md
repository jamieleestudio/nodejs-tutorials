# mastra-evals — 评估：createAnswerRelevancyScorer（模型评分）+ checks.includes（规则断言）

## 演示内容

评估：createAnswerRelevancyScorer（模型评分）+ checks.includes（规则断言）。

```bash
cd ai/mastra/mastra-evals/mastra-evals
npm install
cp .env.example .env   # 填入 DEEPSEEK_API_KEY
npx tsx src/demo.ts    # 程序化演示
npx mastra dev         # 或启动 dev server + Studio
```

> 分数落库 mastra_scorers 表，Studio → Observability 查看。
